

// Método trabalhoso
function max(a, b) {
	if(a>b ){
		return (a)
 }   
    else{
    	return (b) 
    }
}

// Método clean
function min(a, b) {
	return (a<b ? a : b) // Se não for a é b , em sequência
}